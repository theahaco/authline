{
  "name": "@theahaco/authline-relayer",
  "version": "0.1.0",
  "description": "Hosted authorization relayer for Authline — two HTTP endpoints (ready / authorize) so an exchange can onboard holders of a regulated Stellar asset without touching a Stellar SDK.",
  "license": "Apache-2.0",
  "type": "module",
  "private": true,
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "scripts": {
    "build": "npm run build -w @theahaco/authline --prefix ../.. && tsc -p tsconfig.build.json",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^14.4.3",
    "@theahaco/authline": "*"
  }
}
